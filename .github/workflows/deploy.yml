name: Test checkout

on:
  push:
    branches: [main]

jobs:
  test-checkout:
    runs-on: self-hosted
    steps:
      - name: Lấy mã nguồn
        uses: actions/checkout@v3

      - name: In thư mục
        run: ls -la
