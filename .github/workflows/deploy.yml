name: CI/CD with Self-Hosted Runner

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Lấy mã nguồn
        uses: actions/checkout@v3

      - name: Cài dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Chép thư mục build sang production
        run: |
          rm -rf /projects/FaceBugFE/*
          mkdir -p /projects/FaceBugFE
          cp -r dist/* /projects/FaceBugFE/
