name: Test Self-hosted Runner

on:
  push:
    branches: [main]

jobs:
  echo-test:
    runs-on: self-hosted
    steps:
      - name: In Hello World
        run: echo "Hello from self-hosted runner!"
