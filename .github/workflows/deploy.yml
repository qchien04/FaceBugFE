name: Test checkout

on:
  push:
    branches: [main]

jobs:
  test-checkout:
    runs-on: self-hosted

    steps:
      - name: In đường dẫn thư mục hiện tại
        run: pwd

      - name: In toàn bộ nội dung thư mục hiện tại
        run: ls -lah